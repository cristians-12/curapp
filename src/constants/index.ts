/** Nombre visible de la app. */
export const APP_NAME = 'CurApp';

/** URL base del backend. Reemplazar por la URL real. */
export const API_BASE_URL = 'https://api.example.com';

/** Timeout (ms) para peticiones HTTP. */
export const REQUEST_TIMEOUT = 15000;

/** Servicio de Keychain donde se guardan los tokens. */
export const KEYCHAIN_SERVICE = 'curapp.auth.tokens';
